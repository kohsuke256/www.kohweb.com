---
import DescriptionDetails from "./DescriptionDetails.astro";

interface Props {
  etc?: boolean;
}

const { etc } = Astro.props;
---

<div
  class="bg-silver text-steel col-1 grid place-items-center rounded-s-sm px-4 py-[0.5lh] font-bold"
>
  <slot name="term" />
</div>
<div
  class="descriptions bg-iron col-2 flex flex-col justify-center rounded-e-sm px-4 py-[0.25lh]"
>
  <slot name="descriptions" />
  {etc && <p class="opacity-75">etc.</p>}
</div>
