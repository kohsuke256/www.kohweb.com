---
interface Props {
  href: string;
  [key: string]: any;
}

const { href, ...attrs } = Astro.props;
---

<a
  class="text-accent-600 bg-linear-to-r from-[currentColor] to-[currentColor] bg-size-[100%_1px] bg-bottom bg-no-repeat"
  href={href}
  {...attrs}
>
  <slot />
</a>
